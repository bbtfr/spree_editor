<%= javascript_include_tag 'admin/tiny_mce/tiny_mce.js' %>

<script type="text/javascript">
  $(function() {
    var ids = [<%= raw ids.map{|id| "'textarea##{id}'"}.join(', ') %>]
    $(ids.join(', ')).each(function() {
      CKEDITOR.replace(this.id);
    });
    tinyMCE.init();
  });
</script>
